{
	"name": "post-it-board-server",
	"version": "1.0.0",
	"scripts": {
		"server": "ts-node src/server.ts",
		"dev": "echo \"🫣 Wrong folder?: 'npm run dev' should be run in ./client directory\" && echo \"💡 Try: 'cd client && npm run dev'\" && exit 1"
	},
	"dependencies": {
		"express": "^4.18.2",
		"cors": "^2.8.5"
	},
	"devDependencies": {
		"@types/express": "^4.17.17",
		"@types/cors": "^2.8.13",
		"ts-node": "^10.9.1",
		"typescript": "^5.0.0"
	}
}
